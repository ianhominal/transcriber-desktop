<Window x:Class="AudioTranscriber.App.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="Height" Width="420" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource Surface}" FontFamily="Segoe UI" FontSize="14"
        Foreground="{DynamicResource TextPrimary}" Title="Audio Transcriber">
    <StackPanel Margin="20">
        <TextBlock x:Name="Msg" Style="{StaticResource SubtitleText}" Margin="0,0,0,10"/>
        <TextBox x:Name="Input" Style="{StaticResource InputBox}" Padding="8,7"
                 KeyDown="OnInputKeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancelar" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Aceptar" Style="{StaticResource PrimaryButton}"
                    IsDefault="True" Click="OnOk"/>
        </StackPanel>
    </StackPanel>
</Window>
