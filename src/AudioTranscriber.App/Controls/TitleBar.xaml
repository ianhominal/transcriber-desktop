<UserControl x:Class="AudioTranscriber.App.Controls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             x:Name="Root"
             Height="36" Focusable="False">

    <!-- Custom title bar reused across every top-level window (see MainWindow/SettingsWindow/
         SyncWindow). Paired with WindowChrome on the owning Window (CaptionHeight must equal this
         control's Height, 36): WindowChrome automatically treats anything in that strip as a
         draggable caption (drag to move, double-click to maximize/restore) UNLESS the element is
         marked shell:WindowChrome.IsHitTestVisibleInChrome="True" — only the three buttons opt out
         of that below, so they still receive normal clicks. Uses the app's semantic theme brushes
         (Surface/Border/TextSecondary/Danger, via DynamicResource) so it repaints automatically
         with ThemeManager, exactly like the rest of the app. -->

    <UserControl.Resources>
        <!-- Base title-bar button: flat, square, subtle hover using the theme's surface tones. -->
        <Style x:Key="TitleBarButtonBase" TargetType="Button">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonSurface" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource SurfaceAlt}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource SurfacePressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button: same red-hover pattern as IconButtonDanger (see Styles/Buttons.xaml), the
             standard Windows "close goes red on hover" convention. -->
        <Style x:Key="TitleBarCloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButtonBase}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonSurface" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource Danger}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonSurface" Property="Background" Value="{DynamicResource DangerHover}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1">
        <Grid Height="36">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon + title: left as plain (non-hit-test) content so WindowChrome keeps treating
                 this whole area as the draggable caption. -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,8,0"
                        VerticalAlignment="Center" IsHitTestVisible="False">
                <Image Source="{Binding IconSource, ElementName=Root}" Width="16" Height="16" Margin="0,0,8,0"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Text="{Binding TitleText, ElementName=Root}" VerticalAlignment="Center"
                           FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>

            <!-- Minimize / maximize-restore / close: each needs IsHitTestVisibleInChrome="True",
                 otherwise WindowChrome swallows their clicks as caption drag/double-click. -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="MinimizeButton" Content="&#x2015;" Style="{StaticResource TitleBarButtonBase}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Click="OnMinimizeClick" AutomationProperties.Name="Minimizar"/>
                <Button x:Name="MaximizeRestoreButton" Content="&#x2610;" Style="{StaticResource TitleBarButtonBase}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Click="OnMaximizeRestoreClick" AutomationProperties.Name="Maximizar"
                        Visibility="{Binding ShowMaximizeRestore, ElementName=Root, Converter={StaticResource BoolToVisibility}}"/>
                <Button x:Name="CloseButton" Content="&#x2715;" Style="{StaticResource TitleBarCloseButton}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Click="OnCloseClick" AutomationProperties.Name="Cerrar"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
